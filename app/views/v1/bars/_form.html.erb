<%= form_for Bar.new ,:url => {:action => :create, :controller => :bars} do |form| %>
<%= form.label :name %>:
<br><br> <%= form.text_field :name, id: 'bar-name-id' %><br>
	<%= form.submit %>
<% end %>

<%= javascript_include_tag 'input' %>
